<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.interviewassistant.server.mapper.BalanceTransactionMapper">
    <resultMap id="BalanceTransactionResultMap" type="com.interviewassistant.server.entity.BalanceTransaction">
        <id column="id" property="id" />
        <result column="user_id" property="userId" />
        <result column="type" property="type" />
        <result column="minutes" property="minutes" />
        <result column="seconds" property="seconds" />
        <result column="source_type" property="sourceType" />
        <result column="source_id" property="sourceId" />
        <result column="created_at" property="createdAt" />
    </resultMap>

    <sql id="BalanceColumns">
        id, user_id, type, minutes, seconds, source_type, source_id, created_at
    </sql>

    <select id="selectByUserIdOrderByCreatedAtDesc" resultMap="BalanceTransactionResultMap">
        SELECT <include refid="BalanceColumns" />
        FROM balance_transactions
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
    </select>

    <select id="existsBySourceTypeAndSourceIdAndType" resultType="boolean">
        SELECT COUNT(1) &gt; 0
        FROM balance_transactions
        WHERE source_type = #{sourceType}
          AND source_id = #{sourceId}
          AND type = #{type}
    </select>

    <insert id="insert" parameterType="com.interviewassistant.server.entity.BalanceTransaction">
        INSERT INTO balance_transactions (id, user_id, type, minutes, seconds, source_type, source_id, created_at)
        VALUES (#{id}, #{userId}, #{type}, #{minutes}, #{seconds}, #{sourceType}, #{sourceId}, #{createdAt})
    </insert>
</mapper>
