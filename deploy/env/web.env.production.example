# nod 点头 Web 前端生产环境变量模板
# 使用方法：复制为 web/.env.production 后执行 npm run build。

VITE_API_BASE_URL=https://api.your-domain.com

# 必须与后端 INTERVIEW_ASSISTANT_CLIENT_SECRET 一致。
VITE_CLIENT_SECRET=change-me-generate-32-plus-random-chars

# 生产必须关闭模拟支付入口。
VITE_ENABLE_MOCK_PAYMENT=false
